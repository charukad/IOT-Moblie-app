import 'package:flutter/material.dart';

class ShipmentDataPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Shipment Data'),
      ),
      body: Center(
        child: Text('This is a dummy Shipment Data page.'),
      ),
    );
  }
}
