import 'package:flutter/material.dart';

class CapacityPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Capacity'),
      ),
      body: Center(
        child: Text('This is a dummy Capacity page.'),
      ),
    );
  }
}
